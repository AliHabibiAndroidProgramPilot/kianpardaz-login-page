<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KianPardazLogin" parent="Theme.MaterialComponents.DayNight.NoActionBar">
         <!-- Status bar color. -->
         <item name="android:statusBarColor">@color/gold_primary_variant</item>
         <!-- Font -->
         <item name="fontFamily">@font/bnazanin</item>
    </style>
    <style name="App.Theme.SplashScreen" parent="Theme.KianPardazLogin" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen" >
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_inset</item>
        <item name="postSplashScreenTheme">@style/Theme.KianPardazLogin</item>
    </style>

</resources>